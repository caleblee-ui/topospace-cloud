
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-topospace
spec:
  type: {{ .Values.service.type }}
  selector: {app: topospace}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 8787
