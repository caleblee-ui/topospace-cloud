
# @topospace/graph

```js
import "@topospace/graph";
const el = document.createElement("topospace-graph-v1-1");
el.setGraph(payload);
document.body.appendChild(el);
```

Features: force layout, drag, zoom, pan, edge filters, neighborhood rings, and epsilon-filtration animation.
